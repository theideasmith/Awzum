# Methoc definition

# Method names can end with ? or !
# If no parameters, parenthesis can be skipped.
def no_args?
  print("indeed")
end

no_args?
# => indeed

def call_me!(name, calling)
  print(name, "driiiing!", calling)
end

call_me!("bob", "bill")
# => bob
# => driiiing!
# => bill